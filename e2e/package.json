{
  "name": "polls-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end tests for Polls (owned by /qa)",
  "scripts": {
    "test": "playwright test",
    "test:desktop": "playwright test --project=chromium-desktop",
    "test:mobile": "playwright test --project=mobile-360",
    "report": "playwright show-report"
  },
  "type": "commonjs",
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0"
  }
}
